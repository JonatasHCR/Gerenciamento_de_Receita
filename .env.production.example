# Modelo do .env de PRODUÇÃO — copie para o servidor, em DEPLOY_PATH, como `.env`.
# (NÃO versionar o .env preenchido.)
# A imagem é construída localmente no servidor (sem registry) — não há WEB_IMAGE.

# Banco de produção (host "db" = serviço do docker-compose.prod.yml).
DATABASE_URL=postgres://receita:TROQUE_ESTA_SENHA@db/receita_production
POSTGRES_USER=receita
POSTGRES_PASSWORD=TROQUE_ESTA_SENHA
POSTGRES_DB=receita_production

# Chave para descriptografar config/credentials.yml.enc (conteúdo de config/master.key).
RAILS_MASTER_KEY=

# App
RAILS_MAX_THREADS=5
WEB_PORT=80

# Admin inicial — criado pela task dedicada (NÃO pelo seed de demonstração):
#   docker compose -f docker-compose.prod.yml exec web bin/rails admin:create
# A task lê estas variáveis. Sem ADMIN_PASSWORD ela aborta sem criar nada.
ADMIN_EMAIL=admin@suaempresa.com.br
ADMIN_PASSWORD=

# Backup automático (serviço "backup")
BACKUP_INTERVAL_HOURS=12
BACKUP_KEEP=14
