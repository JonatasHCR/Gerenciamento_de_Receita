<% content_for :title, "Editar #{@cost_center.cr_code}" %>

<div class="max-w-2xl">
  <div class="flex items-center gap-3 mb-6">
    <%= link_to "← Voltar", cost_center_path(@cost_center), class: "text-sm text-gray-500 hover:text-gray-700" %>
    <h1 class="text-xl font-semibold text-gray-900">Editar <%= @cost_center.cr_code %></h1>
  </div>
  <div class="bg-white rounded-xl border border-gray-200 p-6">
    <%= render "form", cost_center: @cost_center %>
  </div>
</div>
